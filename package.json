{
  "name": "revu-vscode",
  "displayName": "revu",
  "description": "Annotate code, export reviews to AI",
  "version": "0.1.0",
  "publisher": "kud",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/kud/revu-vscode"
  },
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "icon": "assets/revu-logo.png",
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "revu.addComment",
        "title": "Add Annotation",
        "category": "revu",
        "icon": "$(comment-add)"
      },
      {
        "command": "revu.createNote",
        "title": "Start Discussion",
        "category": "revu",
        "enablement": "!commentIsEmpty"
      },
      {
        "command": "revu.copyToClipboard",
        "title": "Copy to Clipboard",
        "category": "revu",
        "icon": "$(clippy)"
      },
      {
        "command": "revu.exportReview",
        "title": "Export Review",
        "category": "revu",
        "icon": "$(export)"
      },
      {
        "command": "revu.clearComments",
        "title": "Clear all annotations",
        "category": "revu",
        "icon": "$(trash)"
      },
      {
        "command": "revu.cycleView",
        "title": "Cycle View Mode",
        "category": "revu",
        "icon": "$(list-tree)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "revu",
          "title": "revu",
          "icon": "assets/revu-icon.svg"
        }
      ]
    },
    "views": {
      "revu": [
        {
          "id": "revu.notes",
          "name": "Notes"
        }
      ]
    },
    "menus": {
      "editor/title": [
        {
          "command": "revu.addComment",
          "group": "navigation"
        }
      ],
      "editor/context": [
        {
          "command": "revu.addComment",
          "group": "revu"
        }
      ],
      "comments/commentThread/context": [
        {
          "command": "revu.createNote",
          "when": "commentController == revu && !comment",
          "group": "inline"
        }
      ],
      "view/title": [
        {
          "command": "revu.cycleView",
          "group": "navigation@1",
          "when": "view == revu.notes"
        },
        {
          "command": "revu.copyToClipboard",
          "group": "navigation@2",
          "when": "view == revu.notes"
        },
        {
          "command": "revu.exportReview",
          "group": "navigation@3",
          "when": "view == revu.notes"
        },
        {
          "command": "revu.clearComments",
          "group": "navigation@4",
          "when": "view == revu.notes"
        }
      ]
    }
  },
  "scripts": {
    "build": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node",
    "watch": "npm run build -- --watch",
    "typecheck": "tsc --noEmit",
    "package": "vsce package --no-dependencies",
    "install-ext": "npm run build && npm run package && code --install-extension revu-vscode-*.vsix"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/vscode": "^1.90.0",
    "@vscode/vsce": "3.7.1",
    "esbuild": "^0.25",
    "typescript": "^5"
  }
}
